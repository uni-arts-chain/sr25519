# frozen_string_literal: true

RSpec.describe Sr25519 do
  it "has a version number" do
    expect(Sr25519::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
